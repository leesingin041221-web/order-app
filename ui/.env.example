# 로컬 개발 (npm run dev)
VITE_API_BASE=http://localhost:3000/api

# 프로덕션 빌드 (npm run build) — ui/.env.production 에서 자동 적용
# VITE_API_BASE=https://order-app-ac6k.onrender.com/api

# Render 대시보드에서 따로 설정할 경우 위와 동일한 값 사용
